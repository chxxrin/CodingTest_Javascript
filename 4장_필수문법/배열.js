// 배열 선언
const arr = [1,2,3];

// 배열 요소 접근
console.log(arr[0]);
console.log(arr[2]);

// 배열 길이 구하기
console.log(arr.length);

// 배열 요소 추가
arr.push(4);
console.log(arr);

// 배열 요소 제거
const value = arr.pop();
console.log(value);
console.log(arr);